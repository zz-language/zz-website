---
/**
 * Starlight `Header` override: renders the single shared Navbar
 * (NavInner) instead of Starlight's default header. Starlight's own
 * Pagefind search button is intentionally omitted — the global command
 * palette covers docs search, so only one search control exists.
 */
import NavInner from '../header/NavInner.astro';
---

<NavInner />
